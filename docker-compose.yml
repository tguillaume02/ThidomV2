version: "3.8"

services:
  backend:
    build: ./backend
    container_name: thidomv2-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - thidomv2-data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/thidomv2.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: thidomv2-frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/ssl:/usr/local/apache2/ssl:ro
    depends_on:
      - backend
    restart: unless-stopped

  # Optional: InfluxDB for time-series history
  # influxdb:
  #   image: influxdb:2.7
  #   container_name: thidom-influxdb
  #   ports:
  #     - "8086:8086"
  #   volumes:
  #     - influxdb-data:/var/lib/influxdb2
  #   environment:
  #     - DOCKER_INFLUXDB_INIT_MODE=setup
  #     - DOCKER_INFLUXDB_INIT_USERNAME=thidom
  #     - DOCKER_INFLUXDB_INIT_PASSWORD=thidom-password
  #     - DOCKER_INFLUXDB_INIT_ORG=thidom
  #     - DOCKER_INFLUXDB_INIT_BUCKET=thidom_history
  #     - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=thidom-influx-token

volumes:
  thidomv2-data:
  # influxdb-data:
